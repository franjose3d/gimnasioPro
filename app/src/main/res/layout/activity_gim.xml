<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_green_background"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    tools:context=".GimActivity">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/darker_green_header"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="16dp"
        android:paddingTop="32dp">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_volver"
            android:src="@drawable/ic_arrow_back"
            android:scaleType="centerInside"
            android:clickable="true"
            android:focusable="true" />

        <TextView
            android:id="@+id/tvHeader"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="@string/btn_gim"
            android:textColor="@color/accent_blue"
            android:textSize="22sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Calendar Container - Centrado -->
    <FrameLayout
        android:id="@+id/calendarContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:layout_marginHorizontal="16dp"
        android:layout_weight="1" />

    <!-- Question Text -->
    <TextView
        android:id="@+id/tvQuestion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingVertical="12dp"
        android:text="@string/question_what_today"
        android:textColor="@color/text_on_background"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Bottom Buttons Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="16dp"
        android:paddingBottom="8dp">

        <Button
            android:id="@+id/btnMiRutina"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:background="@drawable/bg_card_trainer"
            android:elevation="8dp"
            android:stateListAnimator="@null"
            android:text="@string/btn_mi_rutina"
            android:textColor="@color/text_on_button"
            android:textSize="16sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnNuevaRutina"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:background="@drawable/bg_card_trainer"
            android:elevation="8dp"
            android:stateListAnimator="@null"
            android:text="@string/btn_nueva_rutina"
            android:textColor="@color/text_on_button"
            android:textSize="16sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- ====== BANNER ADMOB - Parte inferior ====== -->
    <FrameLayout
        android:id="@+id/adContainerGim"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:visibility="gone"/>

</LinearLayout>